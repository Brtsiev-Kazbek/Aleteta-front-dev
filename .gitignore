node_modules
.next
out
build
*.log
.env*.local
# Секреты исполнителя: здесь лежит ключ к маршрутизатору. Правило отдельное,
# потому что имя файла — просто `.env`, и под `.env*.local` оно не подходит.
supabase/functions/.env
.DS_Store
next-env.d.ts
# Кеш проверки типов: переписывается каждой сборкой, в истории от него шум.
*.tsbuildinfo
.claude
# Собирается из docs/ на этапе сборки — в истории от него шум.
lib/docs/content.generated.ts
